{
    "$id": "https://github.com/edatasheets/edatasheets.github.io/blob/main/part-spec/currentConsumption.json",
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "Specifications to capture current consumption data",
    "currentConsumption": {
        "type": "object",
        "properties": {
            "supplyName": {
                "description": "Name of the power supply ",
                "type": "string"
            },
            "quiescentCurrent": {
                "description": "quiescent current (Iq) of a device",
                "comment": "units of amps",
                "$ref": "unit.json#/unit"
            },
            "shutdownCurrent": {
                "description": "shutdown current (Isd) of a device",
                "comment": "units of amps",
                "$ref": "unit.json#/unit"
            },
            "activeCurrent": {
                "description": "current consumption when a device is in active mode",
                "comment": "units of amps",
                "$ref": "unit.json#/unit"
            },
            "sleepCurrent": {
                "description": "current consumption when a device is in sleep mode",
                "comment": "units of amps",
                "$ref": "unit.json#/unit"
            },
            "idleCurrent": {
                "description": "current consumption when a device is in idle mode",
                "comment": "units of amps",
                "$ref": "unit.json#/unit"
            }
        }
    }
}

   
      
